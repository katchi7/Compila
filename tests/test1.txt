
typedef myType {
    int size,
    char[50] name,
    float price
}

def main() : void {
    x : int $
    y = 50.55 : float $
    type : myType$
    type.name = "Mytype"$
    ( type == 5 ) -> {
        type = 500 $
    }
    ((x > y) or (x == 5) ) -> {
        out << getLen(type) $
    }
    else -> {
        out << "no" $
    }
}

def getLen( myType type ) : int {
    return type.name.length $
}